<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Adita.PlexNet.WinUI.Dialogs"
    xmlns:dialogContainers="using:Adita.PlexNet.WinUI.Dialogs.DialogContainers">

    <Style TargetType="dialogContainers:DialogContainerBase">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="dialogContainers:DialogContainerBase">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        RequestedTheme="{TemplateBinding RequestedTheme}">
                        <ContentPresenter Grid.Row="1"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
